---
layout: post
title: "Transformer与“未来”？"
date: 2026-06-26
---
# Transformer与“未来”？ <span class="animated-dot"></span>

从一块为了让《雷神之锤》画面更流畅的显卡，到如今给无数学生和打工人续命的 AI，像是一场由电子游戏、脑洞大开的程序员以及神秘皮衣男共同编织的一个美丽意外。


---

## 英伟达与GPU

上世纪 90 年代，3D 游戏开始风靡。但是像《毁灭战士》（Doom）或《毁灭公爵 3D》这样的游戏，本质上是“伪 3D”（2.5D）。虽然墙壁是 3D 的，但怪物和道具其实是一张张平面的贴图纸片（Sprite）。

后来，[《雷神之锤》](https://store.steampowered.com/app/2310/Quake/)（Quake，1996年发布）彻底抛弃了纸片，游戏里的所有怪物、武器、房间架构全部由真实的 3D 多边形（Polygons）构成。

那么代价是什么呢？计算量呈指数级暴涨，当时的 CPU 不出意外地遇到了计算瓶颈。为了处理 3D 游戏中的多边形的位置和光影，需要同时计算成千上万个位置的顶点和像素。虽然 CPU 精通微积分和各种复杂的逻辑控制，但是因为他只有几个核心，只能排队一个一个算。

有人看准了机会。1999 年，[英伟达（NVIDIA）](https://www.nvidia.com/)发布了 **GeForce 256**。

> 英伟达在这款产品上[首次提出](https://blogs.nvidia.com/blog/first-gpu-gaming-ai/)了 **GPU（Graphics Processing Unit，图形处理器）** 的概念。

与侧重低延迟复杂逻辑控制的 CPU 不同，GPU 采用的是高吞吐量的架构设计。它精简了控制单元与缓存，转而集成了数以千计的算术逻辑单元（ALU）。虽然单个核心的独立控制能力有限，但极其擅长执行数据并行（Data Parallelism）计算。这种天生的**大规模并行计算**架构，专为图形渲染和大规模矩阵运算而生。

---

## 数据的伪装

随着 GPU 的并行计算性能与带宽逐渐超越同时期的 CPU，高性能计算（HPC）领域的学者与研究人员开始尝试将这股算力引入非图形领域，用于物理模拟、气象预报及金融建模。

然而，当时的 GPU 缺乏通用的计算编程接口，仅支持 OpenGL 和 DirectX 等标准图形 API。为了突破这一限制，研究人员建立了一套精妙的映射（Mapping）机制，将通用计算任务强行嵌入图形管线（Graphics Pipeline）中：

- 将待处理的科学数据映射为 **纹理贴图（Texture Maps）** 中的像素颜色值
- 将多维数学矩阵映射为 3D 几何体的 **顶点网格（Vertex Meshes）**
- 将具体的计算算法与数学公式改写为 **像素着色器（Pixel Shader）** 代码

在这种机制下，可怜的 GPU 表面上以为自己还在执行标准的图形渲染流程，而其底层的算术逻辑单元（ALU）实际上已被转化为流处理器，用于求解流体力学等数值计算。这种通过图形 API 驱动通用计算的尝试，正是早期 GPGPU（通用图形处理器） 的技术前身。

---

## 一无所有还是赢者通吃？

英伟达 CEO 黄仁勋敏锐地发现了这个趋势。他决定不再让科学家们偷偷摸摸地骗显卡。

2006 年，英伟达推出了 **CUDA（Compute Unified Device Architecture，统一计算设备架构）**。

[CUDA](https://developer.nvidia.com/cuda/toolkit) 的出现是颠覆性的。它提供了一种通用的编程接口，让程序员可以正大光明地直接用 C/C++ 语言来编写在 GPU 上运行 reduction、矩阵乘法等算法，再也不用费劲地把数据伪装成像素来骗 GPU 了。

然而，这一举动在当时被华尔街视为疯狂的败家行为。为了让所有显卡都支持 CUDA，英伟达的芯片成本飙升，利润大跌，股价一度跌掉 80%。但老黄顶住了压力，这一布线就是六年，直到那个改变命运的夏天。

---

## 初现端倪

2012 年，ImageNet [计算机视觉大赛](https://image-net.org/challenges/LSVRC/2012/)。

多伦多大学的 Alex Krizhevsky 和他的导师 Geoffrey Hinton 带着一个名为 **AlexNet** 的深度卷积神经网络（CNN）参赛，以碾压第二名的绝对优势夺冠。

此前，深度神经网络因为计算量太大，一直被学术界冷落。而 Alex 另辟蹊径，他用了 **两块英伟达的 GTX 580 显卡** 来训练这个模型。

> **AlexNet 的成功向全世界证明了一件事：**
> 神经网络（算法）+ 大数据（数据）+ GPU（算力）= 真正的智能化。

这一刻，沉寂多年的深度学习彻底被 GPU 唤醒，英伟达也从一家“臭打游戏的显卡公司”正式踏上了“AI 算力巨头”的转型之路。

---

## 黄金时代

在 AlexNet 之后的五年里，深度学习迎来了全面大爆发：

* **计算机视觉（CV）领域**：VGG、ResNet 等更深的模型不断刷新人类的视觉极限。
* **生成模型**：GAN（生成对抗网络）横空出世，AI 开始学会“画画”。
* **自然语言处理（NLP）领域**：RNN（循环神经网络）和 LSTM（长短期记忆网络）成为了处理文本和语音的主力军。

然而，此时的 NLP 遇到了一个致命的瓶颈：**RNN 家族是串行结构**。它们必须读完第一个词，才能读第二个词。这种“字字句句按顺序来”的特性，导致它**无法完美享受 GPU 的大规模并行算力**。

---

## 改写规则

为了打破 RNN 的串行枷锁，2017 年，谷歌团队发表了一篇名震天下的[论文](https://arxiv.org/abs/1706.03762)：**《Attention Is All You Need》**，正式提出了 **Transformer** 架构。

Transformer 的核心在于**自注意力机制（Self-Attention）**。它彻底抛弃了循环结构，让模型在训练时可以并行计算序列中各个位置之间的关系，不再像 RNN 那样必须按时间步一步步传递状态。

那么，Transformer 凭什么能做到这一点？

答案藏在它的结构里：输入不再按时间步串行流动，而是通过注意力机制在同一层中并行建立联系。下面我们从整体结构开始，把这个过程拆开来看。

### Transformer 结构

#### 整体结构
原始 Transformer 采用编码器-解码器结构，由输入部分、编码器组、解码器组和输出部分构成，每个编解码器组又由多个相同结构的层堆叠组成。
编码器层大致由多头自注意力层和位置前馈网络层组成，解码器层大致由掩码多头自注意力层、交叉注意力层和位置前馈网络层组成。每个子层外部还会配合残差连接、层归一化和 Dropout，让深层网络训练更稳定。

#### 输入部分  
需要进行词嵌入，把词语转换成向量，随后进行位置编码。位置编码有助于使机器获取词语在句子中的具体位置，结合语境来提高预测准确率。

#### 编码器组  
编码器首先接收词嵌入与位置编码后的向量列表输入(列表大小通常会在一个批次内通过填充对齐)。然后经过多头自注意力层，进行 Add&Norm，随后进入位置前馈网络。位置前馈网络是一个全连接前馈网络，每个位置的词都单独经过这个完全相同的位置前馈网络，其由两个线性变换，即两个全连接层组成。第一个全连接层先通过线性变换拓展向量的维度，然后经过非线性的 ReLU 激活函数，获得更好的处理复杂数据的能力。第二个全连接层是一个无激活函数的纯线性变换层，作用是将向量的维度压缩还原为原始维度。经过位置前馈网络后，最后一个编码器最终输出上下文向量矩阵（Hidden States），它将作为交叉注意力中 $K$ 和 $V$ 的来源。

#### 解码器组  
解码器的掩码多头自注意力层内有两种掩码，填充掩码(Padding Mask)和序列掩码(Sequence Mask)。最底层解码器掩码多头自注意力层的输入是词嵌入与位置编码后的输入，其他层解码器自注意力层的输入则来自于上一个解码器的输出。交叉注意力层接受**编码器产生的 $K$ 和 $V$ 矩阵**，也接受来自**解码器掩码多头自注意力层输出的 $Q$ 矩阵**。交叉注意力层的输出经过 Add&Norm 后便进入位置前馈网络。解码器的位置前馈网络和编码器中的结构功能基本相同。

#### 输出部分
解码器组的最终输出是一个向量，要把这个向量转换为一个词，
就要通过一个线性层(是一个简单的全连接神经网络，其将解码器栈的输出向量映射到词表大小的 logits 向量)，随后经过 Softmax 得到词表概率分布。训练时还可以配合 label smoothing，把真实标签从绝对的 one-hot 分布稍微“抹平”，从而缓解模型过度自信。


#### 细节理解补充
- 最底层编解码器的输入是词嵌入，其他编解码器的输入则是上一个编解码器的输出
- 原始编码器-解码器模型中的解码器通常以右移后的目标序列作为输入，开头可以使用 `<sos>` 符号(start of sequence)，结尾可以使用 `<eos>` 符号(end of sequence)。GPT 这类 Decoder-only 模型则更常被理解为：给定已有上下文，按自回归方式预测下一个 token。
- 不同层编解码器的自注意力层和位置前馈网络层不共享权重
- 残差连接包在每个子层外部，例如多头注意力子层和前馈网络子层外都会有残差连接。它的思想是把子层输入直接加到子层输出上，让信息和梯度更容易在深层网络中流动。
- Dropout 机制通过随机屏蔽部分激活来降低过拟合风险，label smoothing 则通过调整训练目标分布，缓解模型对单一正确标签的过度自信。
- Padding Mask(填充掩码)    
因为每个批次输入序列的长度是不一样的，所以要对输入序列长度进行修正，在较短的输入序列后面补0，截断太长的输入序列。
而填充的0无意义，所以要通过Padding Mask在这些位置的值加上一个非常大的负数，从而经过 Softmax 后，这些位置的概率就会接近 0，减小对结果的影响。Padding Mask在编码器的多头自注意力层，解码器的掩码多头自注意力层以及交叉注意力层使用。
- Sequence Mask(序列掩码)        
只在解码器的掩码多头自注意力层使用，防止解码器看到当前预测词后的词而影响预测结果。产生一个下三角部分值全为0，上三角部分为很大的负数(负无穷)的矩阵，进行矩阵加法后只保留句子预测词及之前的词。
- 自注意力机制     
有时一个词语有多个意思，在句子中的具体意思就需要结合语境来确定，这就是自注意力机制要完成的事情。  
计算两词语的相似性就是两个词语向量的点积，自注意力机制的核心是Scaled Dot-Product Attention(缩放点积注意力)，大致步骤如下：
1. 将词嵌入向量合在一起得到一个输入矩阵，将这个矩阵分别乘以三个可训练的权重矩阵，得到查询矩阵 $Q$、键矩阵 $K$ 和值矩阵 $V$。可以粗略理解为：$Q$ 表示当前位置想查询什么信息，$K$ 表示各位置可以被匹配的特征，$V$ 表示最终被加权汇总的信息内容。
2. 计算分数，将查询向量 $Q$ 与对应的键向量 $K$ 做点积，计算后按需要应用 Padding Mask 或 Sequence Mask。
3. 将分数除以单个注意力头中键向量维度 $d_k$ 的平方根，即使用 $\sqrt{d_k}$ 进行缩放，避免点积过大导致 Softmax 过早饱和。       
4. 将分数通过 Softmax 归一化，归一化函数为 e 的当前值次方除以 e 的所有值的和。
5. 将值矩阵 $V$ 乘以 Softmax 分数，为求和做准备。
6. 权值矩阵求和，产生自注意力层的输出  

- 多头自注意力机制   
多头自注意力会把输入投影到多个不同的注意力头中，每个头都有各自的 $Q$、$K$、$V$ 投影矩阵。这样模型可以在不同表示子空间中同时关注不同的位置关系和语义关系。最后把多个头的输出拼接起来，再经过一个线性变换，就得到了多头注意力层的最终输出。
   
- 残差连接和层归一化(Add&Norm)     
原始 Transformer 使用的形式可以写作 $LayerNorm(x + Sublayer(x))$，其中 $x + Sublayer(x)$ 是残差连接得到的值。残差连接可以保留原始输入，相当于在原始表示的基础上做增量修改；层归一化则让每层的表示更稳定，使训练更容易收敛。


### Decoder-only（GPT-2/3）
#### 输入   
每一个 token 会有词嵌入和位置嵌入，词嵌入对应每一个 token 在词表中的编号，而位置嵌入对应在输入序列中的位置，将这两个向量相加，就得到了输入的嵌入。以上嵌入就作为 Transformer 块的输入。

#### 多头自注意力机制  
前面提到的输入首先会经过层归一化，然后便进入自注意力运算。第一步是给归一化后的表示生成查询向量 $Q$、键向量 $K$ 和值向量 $V$。这三个向量是由输入表示分别与三组权重矩阵相乘并加上偏置得到的。
- 一个 $Q$ 向量会和所有 $K$ 向量做点积，点积可以衡量当前位置与其他位置的匹配程度。这些点积结果会形成注意力分数矩阵，随后除以 $\sqrt{d_k}$ 进行缩放，再经过 Softmax 归一化。归一化后的权重与对应的 $V$ 向量加权求和，就是每个注意力头最后的输出。
- 不同注意力头的输出之后会直接堆叠在一起。在 GPT 中，每个注意力头内部的向量长度等于 C / num_heads。这确保了当我们把它们重新堆叠在一起时，我们能够重新得到原始输入特征维度 C
- 现在得到了自注意力层的输出。这个输出不是直接传递到下一阶段，而是会和进入该子层之前的输入相加，形成残差连接；不同层的残差相加对象，就是该层中对应子层的输入。

#### 前馈神经网络
自注意力的输出会再次经过层归一化，然后进入一组全连接的前馈神经网络，MLP 的结果会再次以残差连接的形式与该子层输入相加。

#### 前面这两个多头自注意力、FFN、归一化和残差连接就组成了一个完整的 Transformer 块

#### 输出
多层堆叠的 Transformer 块输出会经过层归一化，然后通过一个线性层映射到词表大小的 logits。logits 随后可以经过 Softmax 转换成输出概率。生成时还可以设置温度参数来调整概率分布的尖锐程度：温度越高，分布越平滑，采样越发散；温度越低，分布越尖锐，输出越保守。

---

这种架构简直就是为 GPU 量身定制的。至少在训练阶段，序列中大量位置关系可以并行计算，GPU 那些数以万计的核心终于可以火力全开，不再像 RNN 那样必须等待上一个时间步的隐藏状态。

---

## 智能爆发

有了 Transformer 和 GPU 的完美结合，AI 进化开启了加速模式。

### LLM 的大炼丹时代

既然并行度拉满了，科学家们便开始奉行“大力出奇迹”的 Scaling Law（规模法则）。从 GPT-3 到如今统治战局的各种万亿参数多模态大模型，参数量呈指数级暴涨。GPU 从单卡演变成了成千上万块 H100、B200 组成的算力集群。

### 从指点江山到御驾亲征

如果说大模型（LLM）是 AI 的大脑，那么 **Agent（智能体）工具** 就是给大脑装上了手和脚。

现在的 AI 不再只是陪你聊天的打字机，而是能够自主思考、调用工具、拆解任务并执行复杂工作流的数字化员工：

* 它们可以自主浏览网页、调用 API 查数据。
* 能够自动编写、运行并调试代码（如各类 AI 编程助手）。
* 甚至能像人类一样直接操控电脑屏幕（Computer Use）完成复杂的办公流程。

---

## 无用的思考
在本人看来，对于目前基于 Transformer 的 LLM，它们的核心训练目标仍然是预测下一个 token，很多能力建立在已有文本和经验分布之上。也正因如此，当任务需要严格推理、真实世界反馈或者训练数据中缺乏足够痕迹时，模型就可能生成看似合理但并不可靠的内容，也就是我们常说的“幻觉”。    

甚至对目前的整个计算机领域来说，图灵、冯诺依曼架构本质上是确定性状态机。即使加入随机数，也只是伪随机，种子确定，输出就确定。     

我们真正缺的是开放性——系统能提出训练分布之外的问题，能对从未见过的情境形成有效模型。不同于强化学习的为了达成某个具体目的，是否能让 AI 像生物一样，单纯是为了“好奇心”去探索？不论是 LLM 还是具身智能，都任重道远。
